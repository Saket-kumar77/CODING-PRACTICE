int majorityElement(vector<int>& nums) {
  int candi=0;
  int count=0;
  for(int x:nums){
    if(count==0)
      candi=x;
    if(x==candi)
      count++;
    else
      count--;
  }
  return candi;
  }
